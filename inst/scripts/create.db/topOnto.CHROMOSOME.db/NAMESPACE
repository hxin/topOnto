import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(ONT, ONT_dbconn, ONT_dbfile, ONT_dbschema, ONT_dbInfo)

exportClasses(ONTTerms,ONTTermsAnnDbBimap)

exportMethods(ID, Term, Synonym, Secondary, show)
